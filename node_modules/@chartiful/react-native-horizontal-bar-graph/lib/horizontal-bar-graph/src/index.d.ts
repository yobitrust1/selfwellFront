import { VerticalBarGraphConfig } from '../../types';
declare const BarChart: ({ data, labels, height, width, barRadius, barWidthPercentage, barColor, style, baseConfig }: VerticalBarGraphConfig) => any;
export default BarChart;
